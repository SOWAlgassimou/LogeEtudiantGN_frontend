// Configuration des fonctionnalités
export const FEATURES = {
  // Socket.IO - Désactivé temporairement à cause des erreurs CORS
  SOCKET_ENABLED: true,
  
  // Autres fonctionnalités
  NOTIFICATIONS_ENABLED: true,
  FAVORITES_ENABLED: true,
  SEARCH_ENABLED: true
};